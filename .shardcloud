DISPLAY_NAME=Evolution API
DESCRIPTION=Self-hosted WhatsApp REST API and multi-device gateway built on Baileys, with instance management, webhooks and Chatwoot/Typebot integrations.
MEMORY=1024
VERSION=recommended
LANGUAGE=node
CUSTOM_COMMAND=npm run db:deploy && npm run db:generate && npm run build && DATABASE_CONNECTION_URI=$DATABASE npm run start:prod
